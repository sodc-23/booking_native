import React from 'react'

import {
    View,
    TouchableOpacity,
    Text,
    StyleSheet
} from 'react-native'

export default TrainStatusBar=({status, onPress})=>(
    <View style={styles.container}>
    </View>
)

const styles=StyleSheet.create({
    
})